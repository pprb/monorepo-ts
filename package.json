{
  "name": "monorepo-ts",
  "private": true,
  "scripts": {
    "compile": "tsc -b -w -i",
    "eslint": "eslint src/**/**.{ts,tsx}",
    "eslint:fix": "eslint src/**/**.{ts,tsx} --fix"
  },
  "dependencies": {
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
